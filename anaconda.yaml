anaconda_config_version: 1
upstream_sources:
  - github:
      identifier: abseil/abseil-cpp
      use_max_tag: true
      include_regex: ^\d{8}\.\d+$
    packages:
      - libabseil
      - libabseil-tests
